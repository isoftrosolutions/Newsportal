Options -Indexes
RewriteEngine On

# Pass admin through untouched
RewriteRule ^admin(/.*)?$ - [L]

# Pass real files and directories through
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Everything else → front controller
RewriteRule ^ index.php [L]
